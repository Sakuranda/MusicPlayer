# 复制为 deploy/Caddyfile 使用前，把域名换成你自己的
# 免费方案（无需备案）：
#   - DuckDNS: https://www.duckdns.org 注册 xxx.duckdns.org 并指向服务器 IP
#   - sslip.io: 直接用 45.125.33.88.sslip.io（零配置，Caddy 自动申请证书）

music.你的域名.duckdns.org {
    reverse_proxy api:8080
}

navi.你的域名.duckdns.org {
    reverse_proxy navidrome:4533
}
